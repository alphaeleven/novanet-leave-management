'use strict';

var appControllers = angular.module('leaveManagementApp.controllers', ['leaveManagementApp.services']);

// login controller
appControllers.controller('LoginController', ['$scope', 'storage', function ($scope, storage) {
  
}]);

// register controller
appControllers.controller('RegisterController', ['$scope', 'storage', function ($scope, storage) {
  
}]);

// manager controller
appControllers.controller('ManagerController', ['$scope', 'storage', function ($scope, storage) {
  
}]);

// user controller
appControllers.controller('UserController', ['$scope', 'storage', function ($scope, storage) {
  
}]);